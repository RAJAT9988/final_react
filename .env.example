# Backend base URL (no /api suffix). Register calls: {URL}/v1/users/signup
VITE_APP_API_URL=http://localhost:3000
# false = talk to the real backend; true = use MSW fake API in the browser
VITE_APP_ENABLE_API_MOCKING=false
